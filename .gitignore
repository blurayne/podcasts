# secrets — never commit the ElevenLabs API key
.env

# regenerable audio build artifacts — never committed (1+ GB; stems/finals are
# reproducible from the specs). The Pages site tracks its own `audio/` dir instead.
/out/
__pycache__


# downloaded voice-actor demos (copyright-sensitive) — reference only, never commit
/sprachproben/

# reference transcript of the real show (copyright) — never commit
series/quarks-for-kids/woozle-orig.txt

# intro jingle (copyright-sensitive) — keep local, never commit
series/quarks-for-kids/wusl-gusl-intro.mp3
